let nombres = ["Ana", "Juan", "Pedro", "Ana"];
let buscar = "Juan";

if (nombres.includes(buscar)) {
  console.log(`Se ha encontrado el nombre ${buscar}`);
} else {
  console.log(`No se ha encontrado el nombre ${buscar}`);
}